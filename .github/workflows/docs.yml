name: Build and Deploy Docs

on:
  push:
    branches:
      - main  
      - docs

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          activate-environment: transposon_silencing
          environment-file: env.yml
          auto-activate-base: false

      - name: Build docs
        shell: bash -l {0}
        env:
          PYTHONPATH: ${{ github.workspace }}:${{ github.workspace }}/library_analyzer
        run: |
          conda activate transposon_silencing
          cd docs
          make clean
          # Generate API documentation
          sphinx-apidoc -f -o source ../library_analyzer/
          python -c "import sys; print('Python path:', sys.path)"  # Debug Python path
          make html SPHINXOPTS="-v"  # Verbose output for debugging
          # Debug: show the contents of the build directory
          ls -R build/html
          
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs/build/html 